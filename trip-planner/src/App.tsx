import React from 'react'

export default function App() {
  return (
    <div className="min-h-screen bg-gray-50 text-gray-900">
      <header className="p-6 bg-white shadow">
        <h1 className="text-2xl font-semibold">Trip Planner</h1>
      </header>
      <main className="p-6">
        <p>Welcome — this is the React + Vite + TypeScript starter. Next: integrate Firebase, auth, and LLM proxy.</p>
      </main>
    </div>
  )
}
